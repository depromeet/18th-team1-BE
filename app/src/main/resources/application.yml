spring:

  application:
    name: app

server:
  servlet:
    context-path: /api

cloud:
  provider: gcp

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    operations-sorter: alpha
    tags-sorter: alpha
