spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/changeme}
    username: ${DB_USER:changeme}
    password: ${DB_PASSWORD:changeme}
    driver-class-name: org.postgresql.Driver
  jooq:
    sql-dialect: postgres
